---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Partidos de fútbol en vivo hoy" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="bg-white min-h-screen text-black">
    <header class="bg-white">
      <nav class="py-4">
        <div class="flex items-center justify-start">
          <a
            href="/"
            class="text-base font-semibold text-black hover:text-black transition-colors tracking-tight px-4 md:ml-[30px] md:px-0"
          >
            lobtuf
          </a>
        </div>
      </nav>
    </header>
    <slot />
  </body>
</html>

<style is:global>
  html {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
    font-weight: 500;
  }
  
  body {
    color: #000000;
    background-color: #ffffff;
  }
  
  * {
    box-sizing: border-box;
  }
  
  /* Apple-style focus states */
  *:focus {
    outline: none;
  }
  
  *:focus-visible {
    outline: 2px solid #000000;
    outline-offset: 2px;
  }
</style>
